import { headerlogo } from '../assets/images';
import { hamburger } from '../assets/icons';

const Nav = () => {
  return (
    <header>
        <nav>
            <a href="/">
                {/* <img src={headerlogo}/> */}
            </a>
        </nav>
    </header>
  )
}

export default Nav